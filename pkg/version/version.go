package version

// Version is the app-global version string, which should be substituted with a
// real value during build.
var Version = "UNKNOWN"

// AppName is the app-global name string
var AppName = "TreeHub"
